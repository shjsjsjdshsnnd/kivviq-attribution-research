name: Step 9 Inventory Dynamics CI

on:
  push:
    branches:
      - step9/inventory-dynamics
  pull_request:
    branches:
      - step8/product-economics

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install
        run: npm install --no-audit --no-fund
      - name: Architecture boundary
        run: npm run architecture
      - name: Typecheck
        run: npm run typecheck
      - name: Test
        # Step 9 counterfactual traps are CPU-heavy. Keep the full inherited
        # suite, but serialize workers so Vitest's RPC layer is not starved on
        # the two-core hosted runner.
        run: npm test -- --maxWorkers=1
      - name: Build
        run: npm run build
